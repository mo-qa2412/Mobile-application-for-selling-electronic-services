import 'package:flutter/material.dart';

ThemeData darkTheme = ThemeData.from(
  colorScheme: ColorScheme.fromSeed(
    seedColor: const Color(0xFF3B7CAD),
    brightness: Brightness.dark,
  ),
  useMaterial3: true,
);
